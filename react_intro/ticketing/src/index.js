import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; 
import TicketingSystem from './App'; // 

ReactDOM.render(
  <React.StrictMode>
    <TicketingSystem />
  </React.StrictMode>,
  document.getElementById('root')
);
